{
  "name": "@amp/storage-sqlite",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "dependencies": {
    "@amp/engine-spi": "workspace:*",
    "@amp/storage": "workspace:*",
    "better-sqlite3": "^12.2.0",
    "drizzle-orm": "^0.31.0"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "watch": "tsc -b --watch",
    "postinstall": "node -e \"try{require('better-sqlite3');}catch(e){process.exit(1)}\" || pnpm rebuild better-sqlite3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13"
  }
}
